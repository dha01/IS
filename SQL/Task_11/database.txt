create table dbo.test_test
(
	test int identy(1,1) not null,
	[CODE][NVARCHAR](128) not null;
	mem nvchar (max) null;
)

select
	dt.test
	dt.code
from dbo.test dt //фром будет один, остальные таблицы будем присоединять с помощью join
	join dbo.test_two on
	tt.test_two = dt.test
	and
	tt.test_two = dt.test
where 111
	

laasfd,a